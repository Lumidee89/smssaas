@extends('layouts.app') @section('title','Assessments') @section('page_title','Assessments')
@section('content')<div class="space-y-6"><div class="flex items-end justify-between"><div><h2 class="text-3xl font-extrabold">Assessment workflow</h2><p class="text-slate-500">Draft, submit, approve, and publish trusted scores.</p></div><a href="{{ route('assessments.create') }}" class="rounded-xl bg-[#06322C] px-5 py-3 font-bold text-white">+ New assessment</a></div><div class="flex gap-2">@foreach([''=>'All','draft'=>'Draft','submitted'=>'Awaiting approval','approved'=>'Approved','returned'=>'Returned'] as $value=>$label)<a href="{{ route('assessments.index',['status'=>$value]) }}" class="rounded-xl px-4 py-2 text-sm font-bold {{ request('status','')===$value?'bg-[#06322C] text-white':'bg-white text-slate-500' }}">{{ $label }}</a>@endforeach</div><div class="overflow-hidden rounded-2xl bg-white shadow-sm"><table class="w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-400"><tr><th class="p-4">Assessment</th><th>Subject</th><th>Term</th><th>Weight</th><th>Scores</th><th>Status</th></tr></thead><tbody class="divide-y">@forelse($assessments as $assessment)<tr class="hover:bg-slate-50"><td class="p-4"><a href="{{ route('assessments.show',$assessment) }}" class="font-bold text-[#06322C]">{{ $assessment->title }}</a><small class="block text-slate-400">{{ ucfirst($assessment->type) }}</small></td><td>{{ $assessment->subject->name }}</td><td>{{ $assessment->term->name }}<small class="block text-slate-400">{{ $assessment->term->academicYear->name }}</small></td><td>{{ $assessment->weight_percentage }}%</td><td>{{ $assessment->scores->count() }}</td><td><span class="rounded-full bg-slate-100 px-3 py-1 text-xs font-bold">{{ ucfirst($assessment->status) }}</span></td></tr>@empty<tr><td colspan="6" class="p-12 text-center text-slate-400">No assessments created yet.</td></tr>@endforelse</tbody></table></div>{{ $assessments->links() }}</div>@endsection
