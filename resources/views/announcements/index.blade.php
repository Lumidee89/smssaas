@extends('layouts.app') @section('title','Announcements') @section('page_title','Communication')
@section('content')<div class="grid gap-6 xl:grid-cols-[.8fr_1.2fr]"><section class="rounded-2xl bg-white p-6 shadow-sm"><h2 class="text-xl font-extrabold">Create announcement</h2><p class="mt-1 text-sm text-slate-400">Publish to the right school audience.</p><form method="POST" action="{{ route('announcements.store') }}" class="mt-6 space-y-4">@csrf<div><label class="mb-1 block text-sm font-semibold">Title</label><input name="title" required maxlength="255" class="w-full rounded-xl border border-slate-300"></div><div><label class="mb-1 block text-sm font-semibold">Message</label><textarea name="body" required rows="6" class="w-full rounded-xl border border-slate-300"></textarea></div><div class="grid grid-cols-2 gap-3"><select name="audience" class="rounded-xl border border-slate-300"><option value="all">Everyone</option><option value="parents">Parents</option><option value="students">Students</option><option value="teachers">Teachers</option></select><input type="datetime-local" name="expires_at" class="rounded-xl border border-slate-300"></div><label class="flex gap-2 text-sm"><input type="checkbox" name="publish_now" value="1" checked> Publish immediately</label><button class="w-full rounded-xl bg-[#06322C] py-3 font-bold text-white">Create announcement</button></form></section>
<section class="space-y-3"><div><h2 class="text-xl font-extrabold">Announcement history</h2><p class="text-sm text-slate-400">Draft, published, and expired communication.</p></div>@forelse($announcements as $announcement)<article class="rounded-2xl bg-white p-5 shadow-sm"><div class="flex gap-4"><div class="flex-1"><div class="flex flex-wrap gap-2"><span class="rounded-full bg-[#7ED3C4]/20 px-2 py-1 text-xs font-bold text-[#06322C]">{{ ucfirst($announcement->audience) }}</span><span class="rounded-full bg-slate-100 px-2 py-1 text-xs text-slate-500">{{ $announcement->published_at ? 'Published' : 'Draft' }}</span></div><h3 class="mt-3 font-bold">{{ $announcement->title }}</h3><p class="mt-2 whitespace-pre-line text-sm text-slate-600">{{ $announcement->body }}</p><small class="mt-3 block text-slate-400">By {{ $announcement->author->name }} · {{ $announcement->created_at->diffForHumans() }}</small></div><div class="flex flex-col gap-2">@if(!$announcement->published_at)<form method="POST" action="{{ route('announcements.publish',$announcement) }}">@csrf<button class="text-sm font-bold text-[#3A7B72]">Publish</button></form>@endif<form method="POST" action="{{ route('announcements.destroy',$announcement) }}" onsubmit="return confirm('Delete this announcement?')">@csrf @method('DELETE')<button class="text-sm text-red-500">Delete</button></form></div></div></article>@empty<div class="rounded-2xl bg-white p-10 text-center text-slate-400">No announcements yet.</div>@endforelse{{ $announcements->links() }}</section></div>@endsection
